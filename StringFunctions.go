package main

import "fmt"

func main() {
	fmt.Print("String Packages")
	var a String
	// IsDigit()
	// IsSpace()
	// IsLetter()
	// IsLower()
	// IsPunct()
	// ToUpper()
	// ToLower()
	// Compare(a,b) a==b : 0 , a<b : -1 , a>b : +1
	// contains(s, substr)
	// HasPrefix(s, prefix)
	// Index(s, substr)

	// String Manipulation
	// Replace (s, old, new, n): n - no. of instances
	// 	will be replaced
	//TrimSpace(s)
	// Strconv Package:
	// Atoi(s) : converts string s to int
	// Itoa(s) : converts int(base10) to string
	// FormatFloat(f, fmt, prec, bitSize): - converts floating point number to a string
	// ParseFloat (s, bitSize) : converts a string to a floating point number
	//
}
